#include "vm_heap.h"

// Nothing to build yet -- see the header. Kept as its own translation unit, matching the shape
// every other vm/ module has, so the em++ and MSVC builds each compile one object per header
// from the start rather than growing that structure later.
