# Clean-room log

One entry per task, appended and never rewritten: the task, the room, and what was read. The wall
is `docs/phase-7.5-design.md` §3.

| Date | Task | Room | Read | Notes |
| --- | --- | --- | --- | --- |
| 2026-09-24 | survey | dirty | VerseVM runtime, bytecode generator, codegen plugin, library `.native.verse` files | prose report to the lead; no code |
| 2026-09-24 | survey | lead-side | this repository only | no UE file opened |
| 2026-09-24 | T1.1 | dirty | bytecode generator, generated op headers, codegen plugin | produced ops.json; lead reviewed: facts only |
| 2026-09-24 | T1.2 | dirty | probe runs; interpolation lookup in the compiler | facts.md, measured |
| 2026-09-24 | T1.3 | dirty | VerseVM values, equality, float printing; probes | values.md; lead reviewed |
| 2026-09-24 | T1.4 | dirty | VerseVM interpreter, frames, codegen, assembler; this repo's HostCooked.cpp; probes | calls.md, modules.md; lead reviewed |
| 2026-09-24 | T1.5 | dirty | VerseVM interpreter, trail, transactions, runtime errors; probes | failure.md, unification.md; lead reviewed |
| 2026-09-24 | T1.6 | dirty | VerseVM classes, objects, accessors, unions; codegen; probes | objects.md; lead reviewed |
| 2026-09-24 | T1.7 | dirty | VerseVM tasks, events, content scopes; probes | tasks.md; lead reviewed |
| 2026-09-24 | T1.8 | dirty | Verse library .native.verse files and native implementations; probes | natives.md; lead reviewed |
| 2026-09-24 | T1.10 | dirty | this repo's host/Private (our own code) | godot-natives.md; lead reviewed |
| 2026-09-24 | T1.11, T1.12 | lead | this repo's HostSidecar.cpp and HostScript.cpp writers; ops.json; spec prose | sidecar.md, format.md; no VerseVM source |
| 2026-09-24 | M0 | neutral | this repo, godot-cpp, Godot's own web platform files | no VerseVM source |
| 2026-09-24 | T2.1 | dirty | VerseVM procedure, bytecode, emitter, interpreter (unwind, frames, handshake), cell headers, codegen plugin and assembler, Solaris VNI JIT; this repo's host/Private; spec/objects.md; format.md; ops.json | host/Private/HostVbcWriter.*, HostVbcOps.gen.h, tools/gen_vbc_writer.py; format proposals to the lead in prose |
| 2026-09-24 | T3.1 | clean | docs/phase-7.5-design.md, tasks.md, format.md, ops.json, facts.md; spec/calls.md, spec/godot-natives.md, spec/modules.md, spec/sidecar.md (reviewed); spec/objects.md header only (draft, not read further); CLAUDE.md, README.md; include/verse_host_abi.h; src/verse_lexer.{h,cpp}, src/verse_module_map.{h,cpp}; tools/gen_vbc_ops.py, tools/build_lexer_test.py, tools/build_cooked_probe.py, tools/build_module_map_test.py, tools/emsdk_env.py, .gitignore; tests/cooked_probe/cooked_probe.cpp, tests/verse_module_map/verse_module_map_test.cpp | vm/ skeleton (vbc_reader.{h,cpp}, vbc_ops.gen.h, vm_value.h, vm_heap.{h,cpp}, vm_interpreter.{h,cpp}, vm_file_reader.{h,cpp}, vm_abi.cpp); tools/gen_vbc_ops.py --emit-cpp; tools/build_verse_vm.py, tools/build_vm_test.py; tests/vm/verse_vm_test.cpp; CLAUDE.md row, .gitignore; no VerseVM source |
| 2026-09-24 | T1.9 | dirty | every spec file; VerseVM interpreter, references, accessors, live variables, profiling ops; probes | ops.md; lead reviewed and resolved its ten inconsistencies (§15.1) |
| 2026-09-25 | T3.2 | clean | docs/phase-7.5-design.md (§1-§7), tasks.md, format.md, facts.md; spec/values.md, spec/ops.md (§3, §4-§6, §8.3, §9, §15), spec/objects.md (§1-§4, §9-§17), spec/unification.md (§1-§5), spec/failure.md (§6), spec/calls.md (grep for fresh), headings of spec/tasks.md; src/verse_lexer.h, src/verse_module_map.h; include/verse_host_abi.h (VH_API); vm/ skeleton; tools/build_verse_vm.py, tools/build_vm_test.py; tests/vm/verse_vm_test.cpp | vm/vm_value.h, vm_bigint.{h,cpp}, vm_cell.{h,cpp}, vm_heap.{h,cpp}, vm_status.h, vm_number.{h,cpp}, vm_equality.{h,cpp}, vm_values.{h,cpp}; tools/build_vm_test.py globs vm/*.cpp; 237 value cases in tests/vm/verse_vm_test.cpp; no VerseVM source |
| 2026-09-25 | T2.2 | clean | docs/phase-7.5-design.md, tasks.md, format.md, ops.json (all); tools/gen_vbc_ops.py; src/verse_lexer.{h,cpp}, src/verse_module_map.{h,cpp} (style only); the three `.vbc` files the dirty-room writer produced for host_smoke/tests/integration/dodge-the-creeps and their `program.vbc.report.txt`, as data, under the lead's scratchpad -- never gen_vbc_writer.py or any other .py there | tools/vbc_dump.py, the independent reader; no VerseVM source, no writer source. format.md §5.1's optional-operand rule read as if a `value`- or `cell:`-kind optional operand could rely on its own kind's 0-means-absent encoding and skip the u8 present-flag; empirically false against `tests/integration`'s `RaceTwo` (`EndTask`'s Write/Switch, the one op pairing role=optional with kind=register): only decodes to 47 in-range, semantically coherent ops when every optional operand of every kind gets the flag unconditionally. Reported to the lead as a spec correction, resolved in the reader by testing against the real file rather than by reading the writer |
| 2026-09-25 | T3.0 | clean | docs/phase-7.5-design.md, tasks.md, format.md, facts.md; spec/values.md, calls.md, failure.md, objects.md, tasks.md, modules.md, natives.md (all reviewed); spec/godot-natives.md (headings, §7-§8.2); spec/ops.md (§15, §15.1, §16, §17; reviewed mid-task, its §15.1 resolutions applied); CLAUDE.md, README.md; include/verse_host_abi.h; tests/cooked_probe/cooked_probe.cpp; tools/run_tests.py; src/verse_export_plugin.cpp (the manifest format only); tests/host_smoke/*.verse and tests/verse_probe/*.verse (existing fixtures, for Verse syntax patterns -- observed behaviour and code checked into tests/, not VerseVM source); the UE checkout's verse_host.dll, verse_host_runtime.dll and verse_cook.exe (run as opaque binaries through the ABI/CLI, never their source) | tests/vm_conformance/*.verse (14 fixtures, 131 methods), tests/vm_conformance/expected/*.txt (recorded from verse_host_runtime.dll); tools/run_vm_conformance.py; tests/cooked_probe/cooked_probe.cpp (added a StatusName() lookup for the printed call status, no behaviour change); no VerseVM source. Found and reported to the lead: every method of a `task(t)` value (`.Await()`, `.Cancel()`, all eight phase queries) segfaults `verse_host_runtime.dll` at this engine build, though each runs fine against the editor host `verse_host.dll` -- excluded from `tasks_basic.verse` for that reason, cancellation covered structurally instead (a `branch`'s enclosing task cancelling it, no method call on the task value) |
| 2026-09-25 | T3.3 | clean | docs/phase-7.5-design.md, docs/web-vm/tasks.md, format.md; spec/sidecar.md, spec/modules.md (all), spec/ops.md (§13-§17), spec/natives.md (§3), spec/calls.md (§9-§11); vm/ (all); include/verse_host_abi.h; src/verse_runtime.cpp (vh_init descriptor and diagnostic handling), src/verse_export_plugin.cpp (manifest and bindings arguments), src/verse_export_paths.h, src/verse_module_map.h (style), src/verse_api_classes.h (the Godot/Verse class-name table, included for vh_class_base_type); tools/vbc_dump.py, tools/build_verse_vm.py, tools/build_vm_test.py, tools/build_cooked_probe.py, tools/run_tests.py (run_cook); tests/cooked_probe/cooked_probe.cpp, tests/vm/verse_vm_test.cpp; three cooks made with verse_cook.exe into the scratchpad, their verse_classes.json read as data; verse_host_runtime.dll run as an opaque binary through the ABI (tests/class_reads_probe), never its source | vm/vm_json.{h,cpp}, vm_sidecar.{h,cpp}, vm_loader.{h,cpp}, vm_natives.{h,cpp}, vm_runtime.{h,cpp}; vm_abi.cpp, vm_cell.h (decoded ops, native binding), vm_values.{h,cpp} (map_reindex); tests/class_reads_probe/class_reads_probe.cpp, tools/build_class_reads_probe.py; loader, JSON and sidecar cases in tests/vm/verse_vm_test.cpp. Class-read transcripts identical to the UE runtime host on host_smoke, tests/integration and dodge-the-creeps. No VerseVM source |
| 2026-09-25 | T5.7, T5.8 | dirty | VerseVM task binding and runtime-error text provider; Solaris module startup; this repo's host/Private | fixes in host/Private only; nothing under vm/ or docs/web-vm/spec/ |
| 2026-09-25 | the wall | lead | review of what the clean room was allowed to read | **A gap in the wall as first drawn:** the clean room was allowed all of `docs/` and all of `CLAUDE.md`, and both describe the UE host in terms of Epic's engine internals — function and class names, some file-and-line citations, a few behaviours of the engine's loader and error formatter. None of it describes how Epic's interpreter executes bytecode, and no clean task's output shows it being used, but it is exposure. From this date the clean room reads `docs/web-vm/`, this design document and five named sections of `CLAUDE.md` only (design §3). Earlier clean tasks T2.2, T3.0–T3.6 and T3.8 had the wider access; their log rows say what each actually read |
| 2026-09-25 | the wall | lead | T5.3's disclosure | **A structural gap the narrowed reading list cannot close:** Claude Code loads the project's `CLAUDE.md` into every agent's context before its first action, so every clean agent has had all of `CLAUDE.md` — including its descriptions of the UE host in terms of VerseVM's runtime (its loader, its error formatter, its event and native-procedure objects) — whatever its reading list said. None of it describes how Epic's interpreter executes bytecode. Raised with the project owner as a decision |
| 2026-09-25 | the wall | project owner | the gap above | **Decision: accept and log.** The exposure is host-integration facts rather than interpreter semantics; `CLAUDE.md` and the workflow are unchanged, and this log is the record of which agents had it (all of them) |
| 2026-09-25 | T5.6 | neutral | this repo's tests/integration, host/Private (our own code) | measured B42; could not fix without engine headers |
| 2026-09-25 | B42 | dirty | VerseVM construction and default objects; this repo's host/Private | fixed in host/Private; the report to the lead was behavioural |
| 2026-09-25 | T5.7, T5.8 | lead | the dirty agent's report | **Exposure, recorded honestly:** the report named Epic's internal functions for binding the task class and the order in which Solaris binds its error-text provider. That is knowledge of the UE host's load path, not of interpreter semantics, and the lead does not pass it to the clean room; the clean interpreter implements `task(t)` from `spec/tasks.md` and `spec/natives.md` as before. Future dirty reports on host defects are asked to state cause in behavioural terms only |
| 2026-09-25 | T3.4 | clean | docs/phase-7.5-design.md; docs/web-vm/tasks.md; spec/ops.md (all), spec/calls.md (all), spec/objects.md (all), spec/failure.md (all), spec/unification.md (all), spec/modules.md (§4-§5), spec/natives.md (§3-§5.5), spec/godot-natives.md (§1-§4, §7-§8.12, §11-§14), spec/sidecar.md (all), spec/tasks.md (§1-§4.4) -- every one reviewed; include/verse_host_abi.h; vm/ (all); src/verse_api_classes.h (the class-name table, as data); tools/build_verse_vm.py, build_vm_test.py, run_vm_conformance.py, vbc_dump.py (run on the cached conformance cook to read its procedures and classes); tests/cooked_probe/cooked_probe.cpp, tests/vm/verse_vm_test.cpp, tests/vm_conformance/*.verse and expected/*.txt; the cached cook's program.vbc read through vbc_dump.py and `grep -a` for native binding keys, as data | vm/vm_interpreter.{h,cpp} (the switch loop, heap frames, calls and argument adaptation, closures, fast and full failure contexts without the undo log, deferred effects, the entry task, the construction protocol, host-built objects), vm_objects.{h,cpp} (layouts, native rule for every class), vm_marshal.{h,cpp}; vm_natives.cpp (ToString, Err, Abs, Floor/Ceil/Round/Int, Quotient/Mod, ConcatenateMaps, Print, VhAdoptOrMint); vm_abi.cpp (vh_instantiate, vh_instance_call, vh_release_instance); vm_cell.h (FrameCell, TaskCell, ObjectCell layout), vm_heap (handle roots, find_interned), vm_loader (value objects listed), vm_values.cpp (struct copies keep the layout), vm_runtime, vm_status.h (Outcome::Yield); interpreter cases in tests/vm/verse_vm_test.cpp. No VerseVM source |
| 2026-09-25 | T3.5 | clean | the standing clean-room instructions; docs/phase-7.5-design.md (§7); docs/web-vm/tasks.md; spec/failure.md (all), spec/unification.md (§2-§5, §11-§12), spec/ops.md (§2-§3, §7-§9, §10 UnifyField-SetFieldLive, §11 Return/ReturnTrailed, §15-§17), spec/godot-natives.md (§4.4-§4.5, §7, §8.12) -- every one reviewed; include/verse_host_abi.h (ReleaseObject); vm/vm_interpreter.{h,cpp}, vm_cell.h, vm_values.{h,cpp}, vm_heap.h, vm_objects.cpp, vm_abi.cpp, vm_natives.cpp (read only); tests/vm/verse_vm_test.cpp; tools/build_verse_vm.py, build_vm_test.py, run_vm_conformance.py (run) | vm/vm_interpreter.{h,cpp}: one flat undo log with a mark per context beside the deferred-effect log, plus a compensation log (commit keeps records, abort replays newest first then runs compensations newest first); every §6 row recorded (RefSet, SetField, CallSet on arrays and maps, transactional ArrayAdd, FastAppendToArray, InPlaceMakeImmutable, Reset, MoveTrailed, MoveNonComparable, ReturnTrailed, placeholder links); per-entry marks so a nested entry is a transaction; a raise aborts every context innermost first and stops every native a nested entry returned through; `compensate` and `record_mint` for natives. Undo-log cases in tests/vm/verse_vm_test.cpp. No VerseVM source |
| 2026-09-25 | T3.8 | clean | docs/web-vm/tasks.md; spec/natives.md (all, reviewed); spec/ops.md (all, reviewed, §15.1 in particular); format.md (§4, §7-§9); vm/vm_natives.{h,cpp}, vm_natives.h/.cpp existing contents, vm_cell.h, vm_heap.h, vm_value.h, vm_values.h, vm_number.{h,cpp}, vm_bigint.{h,cpp}, vm_objects.{h,cpp}, vm_interpreter.h, vm_loader.h, vm_runtime.{h,cpp}, vm_status.h (read only, none owned by this task edited); tools/vbc_dump.py, run against the T3.0-era cached cook scratch/vm_conformance_cook/program.vbc to read the real message/diagnostic/localizable_*/cubic_bezier_capture_internal classes and confirm binding-key spellings, as data; tests/vm_conformance/natives_math.verse (existing fixture) | vm/vm_math.{h,cpp} (bit ops, Clamp, the 17 float functions, CubicBezierInterpInternal); vm_natives.{h,cpp} extended (Join strings, Warn, weak_map, GetSecondsSinceEpoch, ToDiagnostic, Localize/MakeMessageLiteral/MakeMessageInternal/Join messages, GetCastableFinalSuperClass(FromType)/IsOfType, CanCallerAccessEpicInternal_Impl, GetRandomInt/Float, Easing CubicBezier); one line changed at the lead's request (record_mint) in a section owned by the T3.5 agent. natives_math conformance: 16/16 ok. classifiable_subset (6 functions, 4 methods) not implemented -- see report. Did not re-read docs/phase-7.5-design.md this session; relied on spec/natives.md as the task-named source instead. No VerseVM source |
| 2026-09-25 | T3.6 | clean | the standing clean-room instructions; docs/phase-7.5-design.md; docs/web-vm/tasks.md, format.md; spec/objects.md (all), spec/ops.md (all), spec/values.md (§11-§16), spec/calls.md (§4-§7), spec/sidecar.md (all), spec/godot-natives.md (§3-§4.6) -- every one reviewed; include/verse_host_abi.h (vh_arena, vh_class_default_field); host/Verse/Godot.native.verse (variant), GodotApi.native.verse and GodotClasses.native.verse (grep for the variant builders and readers, and one mirrored accessor property); vm/ (all; vm_natives.cpp read only); tests/vm/verse_vm_test.cpp, tests/vm_conformance/objects_basic.verse and expected/*, tests/host_smoke/host_smoke.cpp (the vh_class_default_field cases), tests/integration/scripts/bindings.verse; tools/run_vm_conformance.py, build_verse_vm.py, build_vm_test.py, vbc_dump.py (run on the conformance cook). **Exposure:** the first --record of objects_more ran a native-field overflow, and verse_host_runtime.dll printed a fatal assertion with a native call stack naming VerseVM interpreter source files, line numbers and function names (no source text); the method was removed and the transcript re-recorded without it | vm/vm_interpreter.{h,cpp}: accessor references (LoadField, RefGet, Freeze/FreezeIfAccessor, RefSet, SetField, CallSet, Call on a reference), deferred setters on the construction token run by UnifyNativeObject and by host construction, native-field int64 conversion on UnifyField/InitializeVar/RefSet, float-type bounds, construct() without blocks; vm_cell.{h,cpp} (AccessorRefCell, SetterChainCell, RefCell.native, frame setter progress); vm_objects (unqualified_name, find_slot_by_unqualified_name); vm_equality (struct fields by name, slot or class constant); vm_runtime + vm_abi (vh_class_default_field over a transient instance, minting suppressed); tests/vm_conformance/objects_more.verse + expected; cast, accessor, native-field and struct-constant cases in tests/vm/verse_vm_test.cpp. No VerseVM source read |
| 2026-09-25 | T5.3 | clean | docs/phase-7.5-design.md (all, §2/§8/§9 closest); docs/web-vm/tasks.md; src/verse_host.{h,cpp}, src/verse_runtime.{h,cpp}, src/verse_export_paths.{h,cpp}, src/verse_host_paths.cpp (property-registration pattern), src/verse_script_language.cpp (grep + `_init`'s frame_budget_ms setting, as a pattern), src/verse_script.cpp (grep for PROPERTY_HINT_ENUM), src/verse_api_classes.h (grep only); SConstruct, gdextension.py; godot-cpp/SConstruct, godot-cpp/tools/godotcpp.py (grep, for BoolVariable's import); include/verse_host_abi.h (VH_API/VERSE_HOST_IMPLEMENTATION, vh_init_desc, the top-of-file extern "C" block); vm/vm_file_reader.h, vm/vm_abi.cpp (every vh_* signature, read only, not edited), vm/vm_runtime.cpp (`Runtime::boot`'s file-reader and cooked/parent-directory rule, read only); tools/build_verse_vm.py; godot-cpp/gen/include/godot_cpp/classes/file_access.hpp (get_length/get_buffer signatures). No VerseVM source, no host/Private | SConstruct (verse_vm option, vm/ sources + VERSE_VM_STATIC + CPPPATH when verse_vm or platform=web); src/verse_host.h/.cpp (VerseHostLibrary::load_static() and all_pointers_assigned(), guarded by VERSE_VM_STATIC, assigning all 41 fields straight from vm/'s extern "C" functions -- vm/ untouched, per this task's file ownership); src/verse_runtime.h/.cpp (verse/runtime/backend project setting, STRING+PROPERTY_HINT_ENUM "host,vm"; load_host()'s export branch picks load_static() when the setting is "vm" or the build has the "web" feature; load_host_internal() gained p_use_vm_backend; a Godot-FileAccess-backed vm_set_file_reader installed before host.Init() on that path; a VERSE_VM_STATIC-only debug_check_vm_backend() for this task's check); src/verse_export_paths.{h,cpp} (data_dir_for_this_build() answers a res:// path on the "web" feature, since verse_data ships inside the .pck there -- T5.4/T6.1 still own the export-plugin side). Checks: `scons target=editor` (no vm) builds; `scons target=template_release verse_vm=yes` builds; `python tools/emsdk_env.py -- scons platform=web arch=wasm32 threads=no target=template_release` builds vm/ in, `godot-verse.nothreads.wasm` is 5,006,140 bytes; a throwaway headless GDScript check (not committed) called the new `VerseRuntime.debug_check_vm_backend()` against an editor+verse_vm=yes build and got `{loaded: true, all_pointers_assigned: true, host_kind: 2}` (VH_HOST_RUNTIME); `python tools/run_tests.py --only integration` on the rebuilt default (host, no vm) editor library: 3 passed, 0 failed, 0 skipped -- no T5.6/B42 failures observed in this run. No VerseVM source read. **Disclosure per the lead's narrowed reading list (2026-09-25):** this session's launch context carried this repo's *entire* CLAUDE.md verbatim as standing project instructions, not fetched by choice -- it was present before this task's first tool call, so every section was in context throughout, well past the five now-allowed ("Commands", "Tests", "Instruments, which are not tests", "Generated files -- never hand-edit", "Conventions"). Nothing in the excluded sections (the ABI/host architecture, "Constraints that break things silently", "Verse itself", the phase-document table, by-hand-findings.md's summary rows, etc.) was VerseVM source or drawn from it, and none of it was used to write vm/ -- this task touched only src/, SConstruct and this log -- but it was read, so it is named here rather than left implicit |
| 2026-09-25 | T6.1, T6.2 | clean | docs/phase-7.5-design.md (§9, §10.1); docs/web-vm/tasks.md (T5.4, T6.1-T6.4 rows); src/verse_export_plugin.{h,cpp}, src/verse_export_paths.{h,cpp}, src/verse_runtime.cpp (read only -- web-forces-vm-backend logic was already T5.3's, untouched); tools/run_tests.py (read whole for run_export/run_export_vm's shape, edited); tools/run_web.py (read, and re-run standalone for diagnosis; untouched), tools/emsdk_env.py (read only); tests/web_smoke/ (export_presets.cfg, and its built export tree under build/web/, as data); tests/integration/export_check.gd, main.tscn, export_presets.cfg (read only); godot-cpp/gen/include/godot_cpp/classes/editor_export_plugin.hpp, dir_access.hpp (API surface only). CLAUDE.md's full text was in context per the same structural disclosure T5.3 logged; only its five allowed sections were used | src/verse_export_plugin.h/.cpp (removed "web" from UNREACHABLE_PLATFORMS; refuse a Web export when `verse/runtime/backend != "vm"`, with a sentence naming the project's actual setting; a new `add_directory_to_pack` helper walks the cook's temp directory and ships it via `add_file` at `res://verse_data` instead of `add_shared_object` when the platform tag is "web"); tools/run_tests.py (`_export_template_dir` factored out of `_export_template`; `_web_export_template` for the `web_dlink_nothreads_release.zip` template; `WEB_PRESET_TEXT`/`_web_backend_project` -- a throwaway copy with `verse/runtime/backend="vm"` appended to project.godot and a Web preset appended to export_presets.cfg, mirroring `_vm_backend_project`, never touching the checked-in file; `stage_extension_web` stages the Windows editor library plus the web-wasm32 library so `generate_gdextension` writes a `web.wasm32.single.nothreads.release` row; `_check_pck` given a `name` parameter so `run_web` reuses it under the `[web]` prefix instead of duplicating the stub/vmodule checks; `run_web`, gated on export mechanics and the game booting (an `[integration]` line reaching the console), not on counts (T6.3); `--only web` added). No VerseVM source, no vm/ edits (vm/ is T5.1/T5.2's right now; rebuilt `scons target=editor`, `target=template_release` and `python tools/emsdk_env.py -- scons platform=web arch=wasm32 threads=no target=template_release` against their in-progress vm/ to verify the plugin change, all three succeeded). Checks: `run_tests.py --only export` still 2 passed (export, export-vm), unchanged from before this task. `run_tests.py --only web`: every export-mechanics assertion passes (no host DLL; all 32 `.verse` files stubbed to one byte and both `.vmodule` markers shipped whole via the reused `_check_pck`; `verse_data` confirmed inside `index.pck` at `res://verse_data` via a second `read_pck` check for `verse_classes.json`), and the game does not boot far enough to print an `[integration]` line within 90 s in headless Chrome, so the layer FAILs by design (T6.3's job). Cross-checked against `tests/web_smoke`'s known-good export (T0.3): Chrome/run_web.py reach its own `_ready()` print in ~15 s, so the pipeline is not at fault. `run_tests.py --only export`'s vm-backend run (native Windows, same vm/) executes real Verse bytecode and reaches specific unimplemented-native errors (`VhCallValueConst` on `GetNode`), so vm/ is not universally broken -- the hang is web/wasm-specific and unexplained past that; reported to the lead rather than investigated inside vm/ |
| 2026-09-25 | T4.1, T4.2 | clean | the standing clean-room instructions; docs/phase-7.5-design.md (§7, §7.5); docs/web-vm/tasks.md; format.md §5; ops.json (task ops); spec/tasks.md (all), spec/ops.md (§0, §1, §12-§17), spec/natives.md (§1-§3.5, §5.10 grep, §7, §8), spec/unification.md §6.1, spec/calls.md §4.3-§4.4, spec/failure.md §9 -- all reviewed; vm/ (all of the interpreter, cells, objects, runtime, abi, natives table); tests/vm/verse_vm_test.cpp; tests/vm_conformance/tasks_basic.verse and its expected transcript; tools/vbc_dump.py output over the conformance cook (B2Driver, TasksPhases, H3Driver, EDriver, TasksArm, the task class) | CLAUDE.md was in context in full as session instructions before the narrowed reading list arrived (its R-ASYNC-4 / "Signals, tasks and awaiting" notes informed the per-instance content scope); nothing else outside the list was opened. No VerseVM source seen. A throwaway event(t) Await/Signal was compiled in to check the task machinery against tasks_basic (19/20 then), and removed before the check |
| 2026-09-25 | T4.3 | clean | the standing clean-room instructions; docs/phase-7.5-design.md (all); docs/web-vm/tasks.md; spec/natives.md (§3, §4, §5.9-§5.10, §7, §8), spec/tasks.md (§1-§5, §10-§14), spec/ops.md (§1-§3, §8.1, §8.3, §9, §10 LoadField-SetFieldLive, §11 Call, §12-§17), spec/godot-natives.md (§8.25, §10) -- all reviewed; include/verse_host_abi.h (vh_tick, vh_tick_stats); vm/ (vm_interpreter, vm_tasks, vm_cell, vm_values, vm_equality, vm_natives, vm_runtime, vm_abi, vm_heap, vm_loader.h, vbc_ops.gen.h); tests/cooked_probe/cooked_probe.cpp (it never calls vh_tick); tests/verse_probe/vm_tasks_probe2.verse, vm_ops_live_probe.verse, vm_tasks_subscribe_probe.verse (Verse fixtures, as examples to write tasks_live from); tests/vm/verse_vm_test.cpp; tests/vm_conformance/tasks_basic.verse + expected; tools/run_vm_conformance.py, build_verse_vm.py, build_vm_test.py, vbc_dump.py (run over the conformance cook: Awaiter, ElemAwaiter, MapAwaiter, F5_Batch, L1_Bind and its closure and loop, SleepBody, the event/classifiable_subset classes, classifiable_subset's NotContains/Contains bytecode), and a grep of program.vbc's string table for native binding keys, as data. CLAUDE.md was in context in full as session instructions from the start, not opened by choice; nothing from outside its five allowed sections was used. No VerseVM source seen | vm/vm_tasks.cpp: event(t) Await/Signal, subscribable_event_intrnl Subscribe/Signal, event_subscription Cancel over NativeState; Sleep and Interpreter::take_due_sleepers on a replaceable monotonic clock; BeginAwait/AwaitSuccess/EndAwait/BeginBatch/EndBatch; await registration and hidden variables; the live-task rule shared by RefSet/RefSetLive/CallSet/CallSetLive/SetField/SetFieldLive. vm_interpreter.{h,cpp} wires the ops (Yield now captures the active scope); vm_cell.{h,cpp} (AwaitRegistration, task await state, NativeStateTag); vm_values element_slot; vm_runtime/vm_abi: vh_tick wakes due sleepers each in its own entry and fills vh_tick_stats (Sleeping, JobsRun, ElapsedSeconds, PeakInstanceTasks). vm_natives.cpp: the ten classifiable_subset natives, find_library_class made public, and IsOfType fixed to read the instance from argument 0 (it read Self, which is false for a module-level native, spec/natives.md §3.3). New fixtures tests/vm_conformance/tasks_live.verse (37 methods) and natives_subset.verse (5) with their UE-host transcripts; 21 new unit cases. Conformance 190 ok, 0 DIFF, all 17 classes |
| 2026-09-25 | T5.4 | clean | the standing clean-room instructions; docs/web-vm/tasks.md; docs/phase-7.5-design.md §9 (only); src/verse_export_plugin.{h,cpp}, src/verse_export_paths.{h,cpp}, src/verse_runtime.{h,cpp} (load_host's backend branch and RUNTIME_HOST_FILENAME, read only), src/register_types.cpp (grep, for when load_host runs); gdextension.py, SConstruct; tools/run_tests.py (run_export, stage_extension, _export_template, EXPORT_* constants); godot-cpp/gen/include/godot_cpp/classes/editor_export_plugin.hpp (the full virtual/method surface, to find there is no "remove a declared dependency" hook). CLAUDE.md was in context in full as session instructions from the start, not opened by choice; nothing outside its five allowed sections was used. No VerseVM source, no host/Private, no vm/ (owned by T4.3, untouched) | src/verse_export_plugin.{h,cpp}: `_export_begin` reads `verse/runtime/backend`, and on "vm" rewrites `res://addons/godot-verse/godot-verse.gdextension` to drop its `[dependencies]` section (the only lever an EditorExportPlugin has over a dependency Godot's own export code adds from that file, since add_shared_object has no "remove" counterpart); `_export_end` restores the original text. SConstruct: `verse_vm` now also defaults on for `platform=windows target=template_release`, so one release build carries both backends (T5.3) and the export layer's default host-backend run needs no second library; measured 2,969,600 bytes without it vs 3,548,160 with (~565 KiB, vm/ still growing). tools/run_tests.py: `run_export_vm` (+`_vm_backend_project`, `EXPORT_VM_ABSENT`, `EXPORT_VM_LAUNCH_TIMEOUT`) exports the same tests/integration project from a throwaway copy with `verse/runtime/backend="vm"` appended to its own project.godot (override.cfg does not work: measured that Godot ignores it while running as the editor/exporter, only a running game sees it, and by the time a launched export could see one it would already have to not ship, since exports strip override.cfg too), asserts neither host DLL ships, and prints the launched game's summary line when there is one. Recorded as its own "export-vm" result so a vm-backend regression can't hide the host-backend "export" result. **Checks, both green:** `python tools/run_tests.py --only export` → export: 519 passed/0 failed/11 skipped (unchanged); export-vm: ships neither DLL, boots (`vh_init` succeeds, the script compiles and attaches, ~300 of ~530 cases run before most fail with "Nil" — most call marshaling and every event/signal/GetNode-family native reachable so far answers `Native is not implemented by this runtime`), then hangs past its 90 s budget with no summary line, consistent with T4.3-era work still not covering every `event(t)`/task path and T5.1/T5.2 not done — reported rather than chased, per this task's scope |
| 2026-09-25 | T3.9 | clean | the standing clean-room instructions; docs/phase-7.5-design.md (§7, §7.3); docs/web-vm/tasks.md, format.md (procedure flags); spec/natives.md (§2.2, §3.8-§3.9, §5.6, §5.9, §6 by grep), spec/godot-natives.md (§3.2, §4, §8.29-§8.34, §12.4) -- all reviewed; include/verse_host_abi.h (ReleaseRef, ReleaseObject, vh_collect_garbage); vm/ (heap, cell, interpreter, tasks, natives, runtime, abi, loader, objects); tests/vm/verse_vm_test.cpp; tests/cooked_probe/cooked_probe.cpp (grep: it never ticks); tests/vm_conformance/natives_math.verse; tools/run_vm_conformance.py, build_verse_vm.py, build_vm_test.py. CLAUDE.md was in context in full as session instructions from the start, not opened by choice; nothing outside its five allowed sections was used. No VerseVM source seen | vm/vm_heap: RootSource (roots, weak sweep, post-collect callbacks), an allocation-count trigger (collect when as many cells were made as survived, floor 65536), owns(); vm/vm_interpreter: the interpreter is a root source (sleepers, batched variables, registers, layouts), sweeps minted_peers and a new adopted_refs table and releases through ReleaseObject(bDiscard 0)/ReleaseRef once the heap is consistent, stack_frames() shared with the error frames, the epoch sample and the random generator moved onto it; vm_natives: CanCallerAccessEpicInternal_Impl walks three frames (spec/natives.md §5.9); vm_objects: layouts are roots; vm_runtime/vm_abi: project_scope always rooted, value_objects dropped after layout, collect_garbage (refused in an entry), vh_tick collects when due, vh_collect_garbage collects, VERSE_VM_GC_STRESS collects after every vh_instantiate/vh_instance_call; tools/run_vm_conformance.py --gc-stress; 16 unit cases and a `--gc-bench` mode in tests/vm/verse_vm_test.cpp. Conformance 190/190, and 190/190 under --gc-stress |
